server:
  port: 8083
spring:
  application:
    name: j-cloud-consumer-feign-hystrix

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
logging:
  level:
    root: INFO
